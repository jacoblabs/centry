type Query {
    hello: String
    findById(id: ID): Document
    findAll : [Document]
}

type Document {
    id : Float
    title: String
    content: String
    origin: String
    topic: String
    createdAt: Float
}