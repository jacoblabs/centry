type Query {
    hello: String
    findById(id: Int): Document
    findByTitle(title: String): Document
    findAll : [Document]
}

type Document {
    id : Float
    title: String
    content: String
    origin: String
    topic: String
    createdAt: Float
}